import {uiShapeMetrics} from "@ui/elements/shape"

export const storybookTheme = Object.freeze({
  stagePadding: uiShapeMetrics.tightGap,
  stageGap: uiShapeMetrics.separatorWidth,
  catalogWidth: 210,
  sectionWidth: 160,
  infoWidth: 440,
  dockHeight: uiShapeMetrics.rowHeight,
})
